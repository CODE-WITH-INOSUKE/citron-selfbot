<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Commands - Selfbot Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #1a202c, #2d3748);
            color: #e2e8f0;
            font-family: 'Inter', sans-serif;
            min-height: 100vh;
        }
        .glow {
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
        }
        .card {
            background: rgba(45, 55, 72, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
    </style>
</head>
<body class="flex items-center justify-center">
    <div class="container mx-auto p-6">
        <h1 class="text-4xl font-bold text-center mb-8 text-blue-400">Command Management</h1>
        <div class="card p-6 rounded-lg glow">
            <h2 class="text-2xl font-semibold mb-4">Available Commands</h2>
            <ul class="space-y-4">
                <% commands.forEach(command => { %>
                    <li class="p-4 bg-gray-800 rounded">
                        <p><strong>Name:</strong> <%= command.name %></p>
                        <p><strong>Description:</strong> <%= command.description %></p>
                    </li>
                <% }); %>
            </ul>
            <a href="/" class="mt-4 inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition">Back to Dashboard</a>
        </div>
    </div>
</body>
</html>